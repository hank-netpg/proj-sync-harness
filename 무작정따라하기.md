# 무작정 따라하기 — proj-sync 설치와 첫 사용

> **컴퓨터를 잘 몰라도 괜찮습니다.**
> 이 문서는 **위에서 아래로 순서대로** 그대로만 따라 하면 끝나도록 만들었습니다.
> 화면에 나온 그대로 **하나씩** 하세요. 건너뛰지 마세요. 그게 제일 빠릅니다.

---

## 📖 이 책(문서)을 보는 법

- **`이렇게 생긴 글자`** 는 컴퓨터에 그대로 입력하거나 클릭하는 것입니다.
- 📌 **잠깐만요!** 상자는 "이때 이걸 조심하세요" 하는 꿀팁입니다.
- ✅ **이렇게 되면 성공!** 상자가 나오면, 그 모습이 나와야 다음으로 넘어갑니다.
- 🆘 막히면 맨 아래 **"이럴 땐 이렇게"** 를 보세요. 그래도 안 되면 **화면을 캡처**해서 담당자에게 보내세요.

<br>

> 📌 **잠깐만요! 이 3가지만 기억하세요**
> 1. 인터넷이 연결돼 있어야 합니다.
> 2. 설치는 10~20분쯤 걸립니다. 중간에 창을 끄지 마세요.
> 3. **"허용하시겠습니까?"** 같은 창이 뜨면 겁내지 말고 **"예"**를 누르세요.

---

# 1부. 🪟 Windows(윈도우) 사용자

## 준비물 — 딱 하나

받은 **`proj-sync.zip`** 파일. (담당자가 메일이나 메신저로 보내줍니다.)
그 안에 설치 파일이 다 들어 있으니 다른 건 미리 깔 필요 없습니다.

---

## 따라하기 01 — 압축 풀기

**01** 받은 **`proj-sync.zip`** 을 바탕화면처럼 **찾기 쉬운 곳**에 둡니다.

<br>

**02** 그 파일 위에서 **마우스 오른쪽 버튼**을 클릭합니다.

<br>

**03** 메뉴에서 **`압축 풀기`(또는 `모두 압축 풀기`)** 를 클릭합니다.

<br>

**04** 나온 창에서 아래쪽 **`압축 풀기`** 버튼을 한 번 더 누릅니다.

<br>

✅ **이렇게 되면 성공!**
`proj-sync` 라는 **폴더**가 하나 생깁니다. 더블클릭해서 열면 안에 이런 것들이 보입니다.

```
📁 proj-sync
   ├─ 🖱️ 윈도우-원터치설치.bat   ← 잠시 후 이걸 실행합니다
   ├─ 📄 proj-sync-setup.sh
   ├─ 📄 무작정따라하기.md          ← 지금 보고 있는 문서
   └─ 📁 proj-sync                 (건드리지 마세요)
```

<br>

> 📌 **잠깐만요!**
> 압축을 풀지 **않고** 그냥 zip 안을 열어서 실행하면 동작하지 않습니다.
> 꼭 **"압축 풀기"** 를 먼저 하세요.

---

## 따라하기 02 — 원터치 설치 실행 (핵심!)

> 🖱️ **설치 방법은 두 가지입니다. 편한 걸 고르세요 (결과는 같습니다).**
>
> | 방법 | 실행할 파일 | 이런 분께 |
> |------|-------------|-----------|
> | **① 그래픽 창 (추천)** | **`윈도우-설치(그래픽).bat`** | 버튼·진행바가 있는 예쁜 창으로 하고 싶다 |
> | **② 검은 콘솔 (안전)** | **`윈도우-원터치설치.bat`** | 그래픽 창이 안 뜨거나, 단순한 게 좋다 |
>
> 아래 설명은 **② 검은 콘솔** 기준입니다. **① 그래픽 창**을 골랐다면
> 창이 열린 뒤 **큰 `▶ 설치 시작` 버튼**을 누르고, 진행바가 100%가 되면
> **`지금 이어서 정보 입력`** 버튼을 눌러 **"따라하기 03"** 으로 넘어가면 됩니다.

<br>

**01** 방금 만든 폴더 안의 **`윈도우-원터치설치.bat`** 을 **더블클릭**합니다.
(그래픽 창으로 하려면 **`윈도우-설치(그래픽).bat`** 을 더블클릭하세요.)

<br>

> 📌 **잠깐만요! 파란 경고창이 떠도 정상입니다.**
> **"Windows의 PC 보호"** 라는 파란 창이 뜨면,
> ① **`추가 정보`** 를 클릭 → ② 아래에 생긴 **`실행`** 버튼을 클릭하세요.
> (이 파일은 안전합니다. 처음 실행하는 파일이라 나오는 안내일 뿐입니다.)

<br>

**02** 검은 창이 열리고 안내가 나옵니다. **아무 키나** 눌러 시작합니다.

<br>

**03** 이제 컴퓨터가 필요한 프로그램을 **알아서 하나씩 설치**합니다.
아래 순서로 진행되는 게 보일 거예요. **그냥 기다리면 됩니다.**

```
[1/6] Git for Windows ...
[2/6] VS Code ...
[3/6] Python ...
[4/6] GitHub CLI ...
[5/6] Claude Code ...
[6/6] proj-sync 플러그인 ...
```

<br>

> 📌 **잠깐만요! 이 창이 뜨면 꼭 "예"를 누르세요.**
> 설치 중에 **"이 앱이 기기를 변경하도록 허용하시겠습니까?"** 창이 여러 번 뜰 수 있습니다.
> 그때마다 **`예`** 를 눌러야 설치가 계속됩니다. **`아니요`** 를 누르면 그 프로그램이 안 깔립니다.

<br>

✅ **이렇게 되면 성공!**
프로그램 설치가 끝나면 **`지금 이어서 하시겠어요? (Y/N) [Y]:`** 라고 물어봅니다.

- 여기서 **`Y`** 를 입력하거나 **그냥 Enter** 를 누르면 → **바로 이어서 "정보 입력(토큰·이름)"** 으로 넘어갑니다.
  잠시 후 **`무엇을 할까요?`** 메뉴가 나오는데, 그건 아래 **"따라하기 03"** 의 **`06`** 부터 그대로 하면 됩니다.
- 아직 GitHub 토큰 준비가 안 됐으면 **`N`** 을 눌러 나중에 해도 됩니다. (플러그인은 이미 깔립니다.)

<br>

> 📌 **잠깐만요! 이런 안내가 나오면 (자주 있는 일이에요)**
> **"이 창을 닫았다가 이 파일을 다시 더블클릭 하세요"** 라고 나올 수 있습니다.
> 이건 **오류가 아닙니다.** 방금 깐 프로그램을 컴퓨터가 인식하려면 한 번 더 실행이 필요할 뿐입니다.
> → 검은 창을 닫고 **`윈도우-원터치설치.bat` 을 다시 더블클릭** 하세요. 그러면 나머지가 마무리됩니다.

<br>

➡️ 이제 **"따라하기 03"** 으로 넘어갑니다.
- 위에서 **`Y`(또는 Enter)** 를 눌렀다면 → 03 의 **[갈래 A]** 대로 **`06`** 부터 하세요.
- **`N`** 을 눌렀거나 창을 닫았다면 → 03 의 **[갈래 B]** 대로 **`01`** 부터 하세요.

<br>

(맥 사용자는 아래 **2부**를 보세요.)

---

# 2부. 🍎 Mac(맥) 사용자

## 따라하기 01 — 압축 풀고 실행하기

**01** 받은 **`proj-sync.zip`** 을 **더블클릭**하면 자동으로 압축이 풀리고 **`proj-sync` 폴더**가 생깁니다.

<br>

**02** 폴더를 열고 **`맥-원터치설치.command`** 를 **더블클릭**합니다.

<br>

> 📌 **잠깐만요! "확인되지 않은 개발자" 라고 뜨면**
> ① 파일 위에서 **마우스 오른쪽 클릭**(또는 `Control` 누르고 클릭)
> ② 메뉴에서 **`열기`** 클릭 → 다시 **`열기`** 클릭
> 이러면 실행됩니다. (한 번만 이렇게 하면 다음부턴 그냥 더블클릭 돼요.)

<br>

**03** 터미널 창이 열리면 **Enter** 를 눌러 시작합니다.
중간에 **암호**를 물으면 **맥 로그인 암호**를 입력하세요. (보안상 화면에 글자가 안 보이는 게 정상입니다. 그냥 치고 Enter.)

<br>

✅ **이렇게 되면 성공!**
프로그램 설치가 끝나면 **`지금 이어서 하시겠어요? (Y/N) [Y]:`** 라고 물어봅니다.
- **`Y`**(또는 Enter) → 바로 이어서 정보 입력으로 → **"따라하기 03"** 의 **[갈래 A] `06`** 부터
- **`N`** → 나중에 → **"따라하기 03"** 의 **[갈래 B] `01`** 부터
(중간에 "다시 더블클릭 하세요" 가 나오면, 창 닫고 그 파일을 한 번 더 더블클릭하면 됩니다.)

---

# 공통. 따라하기 03 — 나만의 정보 입력하기 (토큰·이름)

프로그램은 다 깔렸습니다. 이제 **"내가 누구인지"** 를 한 번만 알려주면 됩니다.

<br>

> 🔀 **여기서 두 갈래로 나뉩니다. 본인 상황에 맞는 쪽만 보세요.**
>
> - **[갈래 A] 설치 창에서 "지금 이어서 하시겠어요? (Y/N)" 에 `Y`(또는 그냥 Enter)를 눌렀다면**
>   → 이미 **`무엇을 할까요?` 메뉴가 화면에 떠 있습니다.** 아래 **"VS Code 로 폴더 열기 / 터미널 열기"는 건너뛰고**,
>   곧바로 **`06`(메뉴에서 `1` 입력)** 부터 이어서 하세요.
> - **[갈래 B] `N` 을 눌렀거나, 창을 이미 닫았다면**
>   → 아래 **`01`(VS Code 로 폴더 열기)** 부터 순서대로 하세요.

<br>

> 📌 **잠깐만요! 왜 채팅으로 안 하고 터미널로 하나요?**
> Claude Code **채팅창에 "실행해줘" 라고 시키면 보안 기능이 막습니다.**
> 그래서 아래처럼 **터미널에 직접** 입력해야 합니다. (어렵지 않아요, 따라만 하세요.)

<br>

### [갈래 B] VS Code 로 폴더 열기

**01** **VS Code** 프로그램을 엽니다. (시작 메뉴/런치패드에서 "VS Code" 검색)

<br>

**02** 위 메뉴에서 **`File`(파일) → `Open Folder`(폴더 열기)** 를 누릅니다.

<br>

**03** 아까 압축을 푼 **`proj-sync` 폴더**를 골라 **`폴더 선택`** 을 누릅니다.

<br>

### [갈래 B] 터미널 열기

**04** 키보드에서 **`Ctrl`** 키와 **`` ` ``**(백틱 — 키보드 왼쪽 위, 숫자 `1` 왼쪽) 를 **함께** 누릅니다.
→ 화면 아래쪽에 **터미널**이 열립니다.

<br>

> 📌 **잠깐만요! [Windows 만] 터미널을 "Git Bash" 로 바꾸세요.**
> 열린 터미널의 첫 글자가 파란 **`PS C:\…>`** 이면 아직 준비가 안 된 것입니다.
> ① 터미널 오른쪽 위 **`∨`**(작은 화살표) 클릭 → **`Select Default Profile`**
> ② 목록에서 **`Git Bash`** 선택
> ③ 터미널 **🗑️(휴지통)** 으로 닫고, 다시 **`Ctrl` + `` ` ``** 로 새 터미널 열기
> ✅ 첫 글자가 **`$`** 로 시작하면 성공입니다.

<br>

### [갈래 B] 실행하기

**05** 터미널에 아래 **한 줄**을 붙여넣고 **Enter** 를 누릅니다.
(붙여넣기: Windows = **오른쪽 클릭**, Mac = **`Cmd` + `V`**)

```bash
bash proj-sync-setup.sh
```

---

### 👇 여기서부터는 [갈래 A]·[갈래 B] 둘 다 똑같습니다

**06** 잠시 뒤 **메뉴**가 나옵니다. 처음이면 **`1`** 을 입력하고 **Enter**.

```
무엇을 할까요?
  1) 초기 세팅 …
  ...
선택 [1]:
```

<br>

**07** 이제 순서대로 물어봅니다. **하나씩** 입력하면 됩니다.

| 순서 | 물어보는 것 | 어떻게 입력? |
|:---:|------------|-------------|
| ① | **GitHub 토큰** | 본인이 발급 (아래 "따라하기 04" 참고) |
| ② | **Slack 봇** | 담당자가 준 `xoxb-…` 를 붙여넣기 |
| ③ | **Notion** | (구버전) 이제 입력하지 않음 — claude.ai Notion 커넥터를 본인이 1회 연동 (v1.21.0) |
| ④ | **구글 드라이브** | 터미널에 `rclone config` 입력 → 브라우저로 구글 로그인 (1회만) |
| ⑤ | **본인 이름** | 이름 일부를 검색 → **번호를 골라** Enter |

<br>

**⑤ 이름 고르기 예시:**
```
이름 검색: 황            ← 본인 성이나 이름 일부
   4) 황한건  (엔지니어링팀 리드)
번호 선택: 4
✓ 선택됨: 황한건
```

<br>

✅ **이렇게 되면 성공!**
마지막에 **`doctor`**(자가진단)가 돌고 **초록색 ✓** 들이 보이면 모든 준비가 끝난 것입니다.

---

## 따라하기 04 — GitHub 토큰 발급 (본인 1회, 약 2분)

위 ①번에서 필요한 **GitHub 토큰**을 만드는 방법입니다.

<br>

> 📌 **잠깐만요!** GitHub 계정이 없으면 먼저 **github.com** 에서 가입하고,
> 담당자에게 본인 아이디를 알려 **조직(ax-harness) 초대**를 받으세요.

<br>

**01** 인터넷 주소창에 **`https://github.com/settings/tokens`** 를 칩니다.

<br>

**02** **`Generate new token`** → **`Generate new token (classic)`** 을 클릭합니다.

<br>

**03** **`Note`** 칸에 아무 이름이나 적습니다. (예: `proj-sync`)

<br>

**04** **`Expiration`(만료)** 은 **`No expiration`** 을 고릅니다.

<br>

**05** **`Select scopes`** 에서 아래 **2개**에 체크합니다. (아주 중요!)
- ☑ **`repo`** (맨 위 큰 항목)
- ☑ **`read:org`** (`admin:org` 항목 안에 들어 있어요)

<br>

**06** 맨 아래 **`Generate token`** 버튼을 누릅니다.

<br>

**07** **`ghp_`** 로 시작하는 긴 글자가 나옵니다. 옆의 **📋 복사** 버튼을 눌러 복사하고,
터미널의 GitHub 입력 칸에 **붙여넣고 Enter** 하세요.

<br>

> 📌 **잠깐만요! 이 화면을 벗어나면 그 글자를 다시 못 봅니다.**
> 놓쳤으면 당황 말고 **01번부터 다시** 만들면 됩니다. (여러 번 만들어도 괜찮아요.)

<br>

✅ **이렇게 되면 성공!** 터미널에 **`✓ 로그인 성공: 본인아이디`** 가 보이면 끝입니다.
`✗ 로그인 실패` 가 나오면 → **05번의 체크 2개**를 빠뜨린 것이니 다시 발급하세요.

---

# 🎉 다 됐습니다! 이제 이렇게 쓰면 돼요

Claude Code(또는 VS Code)를 켜고 채팅창에 아래처럼 입력해 보세요.

- **`/ax:start`** — 이미 있는 사업에 합류하기
- **`/ax:sync`** — 자료 한 번에 동기화 (Slack 받아서 GitHub 에 올리기 등)
- **`@agent-ax:pm`** — "지금 뭐 해야 해?", "진도 점검" 같은 사업관리 질문
- **`@agent-ax:pl`** — "이 문서 초안 만들어줘" 같은 문서 작성

<br>

> 📌 더 많은 사용법은 같은 폴더의 **`MANUAL.md`** 에 있습니다.

---

# 🆘 이럴 땐 이렇게 (자주 막히는 곳)

| 이런 증상이면 | 이렇게 하세요 |
|-------------|--------------|
| 파란 **`PS C:\…>`** 가 보임 (Windows) | 터미널이 Git Bash 가 아님 → **따라하기 03의 "잠깐만요"** 대로 Git Bash 로 바꾸기 |
| **`bash: command not found`** (Windows) | Git 이 아직 안 깔림 → `윈도우-원터치설치.bat` 을 **다시 더블클릭** |
| 빨간 **`$'\r'`** 또는 **`syntax error`** | 아래 줄을 대신 붙여넣기: `bash <(tr -d '\r' < proj-sync-setup.sh)` |
| **"이 앱이 기기를 변경하도록…"** 창 | 겁내지 말고 **`예`** 클릭 (설치에 꼭 필요) |
| Windows 파란 **"PC 보호"** 경고 | **`추가 정보` → `실행`** 클릭 (안전한 파일입니다) |
| Mac **"확인되지 않은 개발자"** | 파일 **우클릭 → `열기` → `열기`** |
| 설치가 중간에 멈춘 것 같음 | 창을 닫고 원터치 설치 파일을 **한 번 더** 실행 (여러 번 해도 안전) |
| **"분류기가 차단"** 메시지 | Claude **채팅** 말고 **VS Code 터미널**에서 실행 |
| **`gh` 로그인이 안 바뀜** | 터미널에 `unset GITHUB_TOKEN GH_TOKEN` 입력 후 다시 시도 |
| 그래도 안 될 때 | **화면을 캡처**해서 담당자에게 보내세요 (제일 빠른 방법!) |

<br>

> 📌 **가장 중요한 마음가짐**
> 실수해도 컴퓨터가 고장 나지 않습니다. **여러 번 다시 해도 안전**하게 만들어져 있어요.
> 막히면 화면 캡처 한 장이면 담당자가 바로 도와드립니다. 편하게 시작하세요! 🙂
