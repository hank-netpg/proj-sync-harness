# proj-sync 시크릿 — 이 파일은 .gitignore 됨 (절대 커밋 금지)
# Slack 봇 토큰 (xoxb-...). api.slack.com/apps → 봇 → OAuth & Permissions
# 팀 공용 토큰을 쓰면 **비워 둔다** — 전역 캐시(~/.proj-sync/credentials)·팀 secrets repo 로 폴백된다.
# 개인 토큰이 필요한 사람만 아래 줄의 주석을 풀고 실제 값을 넣는다.
#   ⚠ 예시값을 그대로 두면 안 된다(issue #20). 값이 있으면 해석이 그것을 채택하려 하고,
#     doctor 는 ✗ 를 띄우며, /ax:sync 1단계가 doctor 라 그 사업의 동기화가 통째로 막힌다.
#PROJ_SYNC_SLACK_BOT_TOKEN=xoxb-...

# Google Drive(대용량 사무파일) 자격증명은 여기 두지 않는다 — rclone 이 보관한다.
# 머신당 1회:  rclone config  →  n(new) → 이름 'gdrive' → drive → 브라우저 인증

# Notion 토큰은 없다 (v1.21.0) — 게시는 로그인한 사용자의 claude.ai Notion 커넥터가 한다.
