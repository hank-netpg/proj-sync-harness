# proj-sync 사용 매뉴얼 — 사업관리 (설치 후)

> 설치·온보딩은 **GUIDE.md** 를 먼저 보세요. 이 문서는 **설치를 마친 뒤** 실제로 사업을 관리하는 방법입니다.
> 핵심: 어려운 건 **PM·PL 에이전트가 알아서** 하고, 여러분은 **물어보고 검토**만 하면 됩니다.

---

## 0. 한눈에 — 두 가지 도구

| 도구 | 부르는 법 | 하는 일 |
|------|-----------|---------|
| **PM 에이전트** | `@agent-ax:pm` | 사업 **관리** — 진도·일정·산출물·위험 점검, "지금 뭐 해야 해?" 답 |
| **PL 에이전트** | `@agent-ax:pl` | 문서 **작성** — RFP 분석, 산출물(제안서·보고서) 초안 |
| **터미널 메뉴** | `bash proj-sync-setup.sh` | 파일 **주고받기** — Slack 다운로드, GitHub/Drive 동기화 |

> ⚠️ **중요**: 파일 다운로드·업로드 같은 작업은 **Claude 채팅창에서 시키면 막힙니다(보안)**.
> 그런 건 **터미널에서 `bash proj-sync-setup.sh`** 로 하세요. 분석·작성·점검은 채팅에서 에이전트로.

---

## 1. 자료가 어디에 저장되나 (자동 분담)

여러분은 신경 쓸 필요 없습니다. 도구가 알아서 나눕니다.

| 종류 | 저장 위치 | 이유 |
|------|-----------|------|
| **마크다운(.md)·소스코드** | GitHub | 텍스트 원본, 버전관리 |
| **문서 게시본** | Notion | 팀이 읽는 곳 |
| **사무파일(한글·워드·PPT·엑셀·PDF)** | Google Drive | 대용량 원본 |
| **소통·알림** | Slack | 대화 |

> 한글/오피스 파일을 올리면 **본문이 자동으로 마크다운으로 변환**되어 PM·PL 에이전트가 읽을 수 있게 됩니다.
> (원본은 Google Drive 에 그대로 보관)

### 산출물 표준 폴더 (정부 단계 기준 — PL이 알아서 배치)
산출물(요구사항명세서·테스트결과 등)은 **사업 단계별 표준 폴더**에 자동 저장됩니다. **여러분이 폴더를 만들거나 기억할 필요 없습니다.**

```
reference/
├── deliverables/              ← 표준 산출물 (전자정부 별표2 기준)
│   ├── 00_영업_제안/           제안요청서분석·제안서·산출내역서·계약서
│   ├── 10_착수/               착수신고서·사업수행계획서·품질/위험관리계획서
│   ├── 20_분석/               요구사항명세서·인터페이스/프로세스/엔티티정의서
│   ├── 30_설계/               메뉴구성도·논리/물리ERD·테이블정의서
│   ├── 40_구현_시험/           단위/통합테스트결과
│   └── 50_종료_인도/           시스템/인수테스트·매뉴얼·완료보고서·준공검사
├── management/                ← 4대 관리 문서
│   ├── risk/                  위험관리대장
│   ├── schedule/              일정
│   └── reports/               PM 진도보고·회의록
└── _extracted/                ← 오피스 파일 변환본 (자동)
```

> PL에게 "요구사항명세서 초안 만들어줘"라고만 하면, **단계(분석)에 맞는 폴더(20_분석)에 알아서** 저장합니다.

---

## 2. 사업 시작하기 — 공고에서 자동으로

### 새 사업 시작 (노션 공고 기준)
회사 Notion **사업 DB**에서 **'제안 진행'으로 결정한 사업**을 골라 PM에게 말하면 됩니다:
```
@agent-ax:pm 노션에서 제안 진행 사업 가져와 새로 시작하자
```
PM이 알아서:
1. Notion DB에서 **제안 진행 사업 목록**을 마감 임박순으로 보여줌
2. 고르면 → **영문 폴더명**(`2026-exa-proj-gamma` 식) + config + 공고요약 자동 생성
3. **사업성격**(B2G-용역/B2G-과제/B2B/내부과제)에 맞는 **표준 산출물 세트**를 자동 적용

> **사업성격이 산출물을 결정합니다**: B2G-용역(별표2 27종) / B2G-과제(R&D 보고서) / B2B / 내부과제.
> 노션 DB의 `사업성격` 필드만 정해두면 PM이 알아서 맞춥니다.

### 데이터가 안 맞으면 PM이 잡아줍니다
사용자가 노션 상태 업데이트를 깜빡해도, PM이 **불일치를 잡아 확인**합니다:
- "마감일이 지났는데 제안 단계예요. 상태가 안 바뀐 건가요?"
- "모니터링엔 제안 중인데 수주실적엔 수행 중이에요. 실제 단계가 뭔가요?"

→ 여러분이 모든 걸 기억할 필요 없습니다. **시스템이 빠진 걸 찾아줍니다.**

### 기존 사업 합류
이미 진행 중인 사업은 터미널 `bash proj-sync-setup.sh` → 초기 세팅에서 Notion DB의 기존 사업을 골라 합류합니다.

---

## 협업 원칙 — 모두 같은 곳을 본다 (시차 없이)

> **파일은 무조건 프로젝트 Slack 채널에 올립니다.** 그러면 누가 언제 합류해도 같은 자료를 봅니다.

```
파일 작업 → 프로젝트 Slack 채널에 업로드 (단일 창구)
   ↓ 에이전트가 받아 로컬 + Drive 저장
   ↓ 같은 파일(내용 동일)은 Drive 에 이미 있으면 안 올림 (자동 중복 제거)
```

- **언제 설치·합류해도 같은 상태**: 합류 시 GitHub 문서 + Drive 사무파일을 한 번에 받아옵니다.
- **중복 걱정 없음**: 같은 파일을 여러 명이 올려도, 내용이 같으면(해시 동일) Drive 엔 한 번만 저장됩니다.
- **작업 전 받기**: 다른 팀원이 올린 게 있으면 PM이 "최신 아니에요, 받으세요"라고 알려줍니다.

> ⚠️ 사무파일(hwp/ppt 등)은 git처럼 자동 병합이 안 됩니다. 같은 파일을 동시에 고치면 PM이 충돌을 경고합니다 → 한 명씩 수정하세요. (Drive 버전 기록으로 이전 버전 복원 가능)

---

## 3. 자료 받고 변환하기

### Slack 채널 자료 다운로드
채팅에서 그냥 시키면 됩니다 (v1.2부터 에이전트가 직접 실행):
```
@agent-ax:pm 슬랙 자료 받아줘        (또는 "동기화 해줘" — 받기부터 백업까지 한 번에)
```
터미널이 편하면 기존대로: `bash proj-sync-setup.sh` → 메뉴 **2) Slack 다운로드/분류**
- 채널의 파일을 받아 **종류별 폴더로 자동 분류**
- 한글/오피스 파일은 **자동으로 마크다운 변환**(PM·PL 이 읽게)

### ⚠️ 한글 파일(.hwp)은 .hwpx 로 올려주세요
구버전 한글(.hwp)은 표·서식이 깨집니다. **한글에서 "다른 이름으로 저장 → .hwpx"** 로 저장해 올리면 표까지 정상 추출됩니다. (.hwp 를 올리면 변환 안내가 표시됩니다)

---

## 4. PM 에이전트로 사업 점검 (가장 많이 씀)

Claude 채팅창에 그냥 물어보세요. 용어 몰라도 됩니다.

```
@agent-ax:pm 진도 점검해줘
@agent-ax:pm 지금 뭐 해야 해?
@agent-ax:pm 마감 임박한 거 있어?
@agent-ax:pm 산출물 상태 알려줘
@agent-ax:pm 주간보고 써줘        (→ /ax:report)
```

PM 이 답하는 것:
- **지금 단계** + 그 단계에 **필요한 산출물**
- **완료/미작성** 산출물 (표준 목록과 대조)
- **지연된 작업** (마감일 지난 것)
- **"지금 할 일" Top 3** (가장 급한 것)

### PM은 4대 관리를 알아서 지휘합니다 (오케스트레이터)
여러분은 PM 하나만 부르면, PM이 뒤에서 **4대 관리를 전부** 점검해 종합합니다:

| 관리 영역 | PM이 자동 점검 |
|-----------|----------------|
| **산출물 관리** | 표준 산출물 ↔ 실제 파일 대조, V-Model 짝(설계↔테스트) |
| **일정·진도** | 완료율(번다운), 지연·마감 임박 |
| **위험 관리** | 위험관리대장, 자동 위험 신호(지연·미할당·누락) |
| **형상 관리** | 산출물 버전·변경(GitHub), 미백업 경고 |

→ "위험은?", "일정은?"처럼 따로 물어도 되고, "전체 점검"하면 4개 다 봅니다. **방법론(Agile+V-Model)은 PM이 흡수**, 여러분은 결과만 봅니다.

> PM 은 **점검·보고·지휘 + 동기화 실행**까지 합니다 (v1.2). 문서 *작성*만 PL 담당. 다운로드·push·Drive 도 "동기화 해줘" 한마디면 PM 이 직접 수행합니다 (표준 스크립트로만 — 비번·토큰은 에이전트가 만지지 않음).

### 주간보고 — 매주 진행+리스크 한 장 (`/ax:report`)
"주간보고 써줘" 하거나 `/ax:report` 를 실행하면, revision(전제·WBS·요구사항추적표·history)을 증류해 **한 장 보고서**를 만들어 Slack 에 올립니다:
- **🔴 리스크** 먼저 — 지연·미확정 전제·"수행중인데 착수 산출물 0건" 같은 신호
- **📊 진척** — 완료율(지난주 대비 추이)·마감 임박·마일스톤 D-day
- **✅ 금주 실적·차주 계획**, **🟡 결정 필요**, **📦 산출물 현황·누락**
- 맨 위 **Slack 발신본**이 그대로 채널에 게시되고, 지연·임박 작업의 **담당자를 멘션**합니다.

**매주 자동 발송**: `claude -p "/ax:report"` 를 cron/launchd 또는 Claude Code 스케줄 루틴에 걸면 매주 자동 게시됩니다(헤드리스: 게시+기록만). 편집은 revision(SSOT)에서 — 보고서는 파생물이라 손대지 않습니다.

---

## 5. PL 에이전트로 문서 작성

```
@agent-ax:pl 이 RFP 분석해줘
@agent-ax:pl 요구사항명세서 초안 만들어줘
@agent-ax:pl 제안서 목차 짜줘
@agent-ax:pl 중간보고서 초안 써줘
```

PL 이 하는 것:
- 확보된 자료(변환된 마크다운)를 **읽고 분석**
- 표준 양식으로 **초안 작성** → `reference/` 폴더에 저장
- 자료 없는 항목은 **`[작성 필요]` 표시** (지어내지 않음)

> 초안은 **여러분이 검토·승인**해야 합니다. PL 이 만든 건 출발점입니다.

---

## 6. 자료 동기화 (작업 후)

**가장 쉬운 방법** — 채팅에서:
```
@agent-ax:pm 동기화 해줘
```
→ Slack 받기 → 분류·변환 → GitHub 백업 → Drive(대용량) → Notion 게시 → 완료 요약까지 에이전트가 끝까지 수행하고, 안 된 단계(예: rclone 미설치라 Drive 스킵)는 이유·조치와 함께 보고합니다.

다른 경로 (결과 동일):
- `/ax:sync` — Claude 커맨드
- 터미널 `bash proj-sync-setup.sh` 메뉴 4) GitHub 동기화
- VSCode: **Terminal → Run Task → AX: …** (v1.2, `/ax:init` 시 자동 설치)

---

## 7. 단계 전환

사업이 다음 단계로 가면:
```
/ax:phase 수행중
```
→ 사업 단계를 바꾸고 회사 Notion 사업총괄목록과 동기화합니다.

---

## 8. 전형적인 하루 흐름

```
[아침]  @agent-ax:pm 진도 점검           → 오늘 할 일 확인
[수신]  @agent-ax:pm 슬랙 확인해줘        → 새 요청·자료 트리아지 (/ax:inbox)
[자료]  @agent-ax:pm 자료 받아줘          → 새 자료·변환 (터미널 메뉴 2 도 가능)
[분석]  @agent-ax:pl 이 문서 분석         → 요약·요건
[작성]  @agent-ax:pl 산출물 초안          → 검토·수정
[정리]  @agent-ax:pm 동기화 해줘          → GitHub·Drive·Notion 백업 (터미널 메뉴 4 도 가능)
```

---

## 9. 자주 막히는 곳

| 증상 | 해결 |
|------|------|
| 에이전트가 다운로드·push 하다가 "차단됐다"고 함 | auto 모드의 자격증명 보호 동작 — **정상**입니다. 에이전트가 알려준 명령 1줄을 터미널에 복붙하거나, auto 모드 해제 후 재지시 |
| PM 이 "WBS 미정의" 라고만 함 | 사업 초기 설정(단계·유형)이 안 됨 → 터미널 메뉴 1 |
| 한글 파일 내용이 깨져 보임 | .hwp → **.hwpx 로 저장해 다시** 올리기 |
| `@agent-ax:pm` 이 안 보임 | Claude Code **재시작** 후 `/agents` 에서 확인 |
| Drive 접속 안 됨 | `gdrive_sync.sh doctor` 가 원인을 E-코드로 짚어줌 (E10 rclone 미설치 / E11 인증 만료 / E12 조직 정책 차단 — 관리자에게 rclone 허용 요청 / E13 remote 미설정 / E14 쓰기 권한) |
| Drive push 가 "충돌(E16)"로 멈춤 | 다른 팀원이 Drive 파일을 더 최근에 수정함 — 목록 확인 후 덮어쓸지(승인) / 먼저 받을지(pull) 선택 |
| VSCode 태스크가 "플러그인 경로 미기록" 오류 | Claude Code 에서 `/ax:doctor` 1회 실행 (셔임 자동 생성) |

---

## 10. 도움말

- **설치·온보딩**: GUIDE.md
- **점검**: 터미널 메뉴 또는 `@agent-ax:pm 전체 점검`
- 막히면 그 화면을 **캡처해서 담당자에게** 보내세요.
